name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: AssetGuardianMobile/package-lock.json

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        cache: 'gradle'

    - name: Install npm dependencies
      working-directory: ./AssetGuardianMobile
      run: npm install

    - name: Build Android APK
      working-directory: ./AssetGuardianMobile/android
      run: |
        export NODE_OPTIONS="--max-old-space-size=4096"
        chmod +x ./gradlew
        ./gradlew assembleRelease --no-daemon

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: AssetGuardian-APK
        path: AssetGuardianMobile/android/app/build/outputs/apk/release/app-release.apk
